{
    "name": "budget-app-production",
    "version": "1.0.0",
    "private": true,
    "engines": {
        "node": ">=18.0.0",
        "npm": ">=8.0.0"
    },
    "scripts": {
        "build": "cd server && npm ci && npm run prisma:generate && npm run build && cd ../client && npm ci && npm run build",
        "start": "cd server && npm run prisma:migrate:deploy && npm start",
        "postinstall": "npm run build"
    },
    "dependencies": {
        "npm": "^8.0.0"
    }
}