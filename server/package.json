{
  "name": "budget-server",
  "version": "1.0.0",
  "description": "Budget App Backend API",
  "main": "dist/index.js",
  "type": "commonjs",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "dev:watch": "nodemon",
    "build": "tsc && npm run prisma:generate",
    "build:production": "npm ci --only=production && npm run prisma:generate && tsc",
    "start": "node dist/index.js",
    "start:ts": "ts-node src/index.ts",
    "test": "npx jest",
    "test:watch": "npx jest --watch",
    "test:coverage": "npx jest --coverage",
    "test-server": "node dist/simple-server.js",
    "prisma:generate": "npx prisma generate",
    "prisma:migrate": "npx prisma migrate dev",
    "prisma:migrate:deploy": "npx prisma migrate deploy",
    "prisma:studio": "npx prisma studio",
    "prisma:reset": "npx prisma migrate reset",
    "seed": "ts-node src/utils/seed.ts",
    "railway:build": "npm install && npm run prisma:generate && npm run build",
    "railway:start": "npm run prisma:migrate:deploy && npm start",
    "railway:full": "npm install && npm run prisma:generate && npm run build && npm run prisma:migrate:deploy && npm start",
    "railway:start:simple": "npm start",
    "railway:dev": "npm run prisma:migrate:deploy && npm run start:ts"
  },
  "dependencies": {
    "@prisma/client": "^6.17.1",
    "@types/exceljs": "^0.5.3",
    "@types/multer": "^2.0.0",
    "@types/node-xlsx": "^0.15.3",
    "bcryptjs": "^2.4.3",
    "chardet": "^2.1.0",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "csv-parser": "^3.2.0",
    "dotenv": "^17.2.3",
    "exceljs": "^4.4.0",
    "express": "^4.18.2",
    "express-rate-limit": "^8.1.0",
    "helmet": "^8.1.0",
    "iconv-lite": "^0.7.0",
    "joi": "^18.0.1",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.2",
    "node-xlsx": "^0.24.0",
    "pdfjs-dist": "^5.4.296",
    "prisma": "^6.17.1"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/compression": "^1.7.2",
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "@types/jest": "^30.0.0",
    "@types/jsonwebtoken": "^9.0.2",
    "@types/node": "^20.19.21",
    "@types/supertest": "^6.0.3",
    "jest": "^30.2.0",
    "nodemon": "^3.0.1",
    "supertest": "^7.1.4",
    "ts-jest": "^29.4.5",
    "ts-node": "^10.9.1",
    "typescript": "^5.9.3"
  },
  "keywords": [
    "api",
    "express",
    "typescript",
    "prisma",
    "jwt"
  ],
  "author": "Your Name",
  "license": "MIT",
  "engines": {
    "node": ">=20.11.1 <23",
    "npm": ">=9.0.0"
  }
}